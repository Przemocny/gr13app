import React from 'react'
import Toggler from './Hooks'

// TDD - test driven design
// po wyrenderowaniu komponent powinien być linkiem i nic nie ma być w srodku - stan
// jeśli w niego kliknę i jest widoczny to powinien się schować - akcja
// jeśli w niego kliknę i jest schowany to powinien się pojawić - akcja


describe('TDD - Toggler - testy działania', () => {
    it('po wyrenderowaniu komponent powinien być linkiem', () => {
        expect(true).toBe(true);
    });

    it('po wyrenderowaniu komponent powinien nie mieć nic w srodku', () => {
        expect(true).toBe(true);
    });
    it('jeśli w niego kliknę i jest widoczny to powinien się schować', () => {
        expect(true).toBe(true);
    });
    it('jeśli w niego kliknę i jest schowany to powinien się pojawić', () => {
        expect(true).toBe(true);
    });
})
